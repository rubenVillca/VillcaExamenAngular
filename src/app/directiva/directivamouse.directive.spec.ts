import { DirectivamouseDirective } from './directivamouse.directive';

describe('DirectivamouseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivamouseDirective();
    expect(directive).toBeTruthy();
  });
});
